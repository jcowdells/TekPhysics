#version 330 core

in vec2 tex_coord_f;

out vec4 colour;

uniform sampler2D atlas;
uniform vec4 text_colour;

void main() {
    vec4 sampled_colour = texture(atlas, tex_coord_f);
    colour = vec4(text_colour.r, text_colour.g, text_colour.b, min(text_colour.a, sampled_colour.r));
}